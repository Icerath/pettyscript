[package]
edition = "2021"
name = "formatter"
version = "0.1.0"

[dependencies]
vm = { version = "0.1.0", path = "../vm" }

[dev-dependencies]
parser = { version = "0.1.0", path = "../parser" }

[lints]
workspace = true
