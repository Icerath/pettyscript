class Point {
    x: float,
    y: float

}

fn Point::new(x: float, y: float) -> Self {
    // TODO - Syntax for class creation
}

fn Point::normalize(self) -> float {
    return (self.x * self.x + self.y * self.y).sqrt()
}

fn Point::__neg__(self) {
    Self::new(self.)
}
