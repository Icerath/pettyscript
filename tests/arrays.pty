let empty_arr: array[int] = [];
assert(f"{empty_arr}" == "[]");
assert(f"{[1, 2, 3]}" == "[1, 2, 3]");
let arr = [1];
assert(arr.pop() == 1);
assert(f"{arr}" == "[]");
arr.push(2);
assert(f"{arr}" == "[2]");