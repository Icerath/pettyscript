// TODO: Fix ugly literal syntax
// TODO: Tuple index (or maybe field access like Rusts .0?)

assert(#[1, 2] == #[1, 2]);
assert(f"{#[1, 2]}" == "[1, 2]");

// TODO: destructuring
// let [[a, b], c]: tuple[tuple[_, char], _] = #[#[1, '2'], "Go!"];
// assert(a == 1);
// assert(b == '2');
// assert(c == "Go!");