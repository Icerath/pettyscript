fn foo[T: Display](val: T, str: str) -> T {
    assert(f"{val}" == "<generic 0>"); // FIXME: `f"{val}" == str`
    return val;
}

let x: int = foo(1, "1");
let y: str = foo("Boo", "Boo");
assert(x == 1);
assert(y == "Boo");
